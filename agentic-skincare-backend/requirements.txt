# ── Core LangChain / LangGraph ───────────────────────────────────────────────
# BLOQUEO: langchain-google-firestore==0.5.0 (ultima version disponible)
# requiere langchain-core<1.0.0. El stack LangChain queda acotado hasta que
# langchain-google-firestore publique soporte para langchain-core>=1.0.
langchain-core>=0.3.85,<1.0.0
langchain>=0.3.27,<1.0.0
langgraph>=0.6.0,<1.0.0

# ── Google Cloud integrations ────────────────────────────────────────────────
# Unico uso de Google que queda: Firestore como vector store del RAG (ver
# features/info_productos/graph.py, scripts/ingestar_pdfs.py). El chat y los
# embeddings ya no usan Vertex AI/Gemini -- ver bloque de AWS/Bedrock abajo.
langchain-google-firestore==0.5.0
google-cloud-firestore>=2.16.0,<3.0.0
# google-cloud-storage: NO actualizar a >=3.0.0
# langchain-google-firestore==0.5.0 requiere google-cloud-storage<3.0.0

# ── Data validation ───────────────────────────────────────────────────────────
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.8

# ── AWS (Lambda entrypoint: entrypoints/lambda_handler.py, repositories/) ────
boto3
# langchain-aws 0.2.x es la ultima serie compatible con langchain-core<1.0.0
# (requerido por langchain-google-firestore==0.5.0 arriba); 1.x requiere
# langchain-core>=1.0. ChatBedrockConverse/BedrockEmbeddings -- mismo patron
# que connect-nlu-router-menu (ver ../context.md), modelo en core/settings.py.
langchain-aws>=0.2.35,<0.3.0

# ── API server ────────────────────────────────────────────────────────────────
fastapi>=0.138.0
uvicorn>=0.49.0

# ── Bedrock AgentCore Runtime SDK ────────────────────────────────────────────
# entrypoints/agentcore_app.py -- BedrockAgentCoreApp. Se despliega via
# scripts/deploy_agentcore.ps1 (requirements-agentcore.txt), pero tambien
# hace falta en el venv local para poder importar/testear ese entrypoint.
bedrock-agentcore

# ── UI / Demo ─────────────────────────────────────────────────────────────────
gradio>=6.19.0

# ── PDF processing ────────────────────────────────────────────────────────────
pdfplumber>=0.11.0

# ── Environment management ────────────────────────────────────────────────────
python-dotenv>=1.0.0
